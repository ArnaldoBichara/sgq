version: '3.4'

# The default docker-compose.override file can use the "localhost" as the external name for testing web apps within the same dev machine. 
# The ESHOP_EXTERNAL_DNS_NAME_OR_IP environment variable is taken, by default, from the ".env" file defined like: 
#     ESHOP_EXTERNAL_DNS_NAME_OR_IP=localhost
# but values present in the environment vars at runtime will always override those defined inside the .env file
# An external IP or DNS name has to be used (instead localhost and the 10.0.75.1 IP) when testing the Web apps and the Xamarin apps from remote machines/devices using the same WiFi, for instance.
services:
  seq:
    environment:
      - ACCEPT_EULA=Y
    ports:
      - "5340:80"

  sql.data:
    environment:
      - SA_PASSWORD=Pass@word
      - ACCEPT_EULA=Y
    ports:
      - "5433:1433"    # Important: In a production environment your should remove the external port
  nosql.data:
    ports:
      - "27017:27017"  # Important: In a production environment your should remove the external port

  rabbitmq:
    ports:
      - "15672:15672"  # Important: In a production environment your should remove the external port
      - "5672:5672"    # Important: In a production environment your should remove the external port

  identity.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:80
      - ConnectionString=${ESHOP_AZURE_IDENTITY_DB:-Server=sql.data;Database=SGQ.Service.IdentityDb;User Id=sa;Password=Pass@word} 
      - SpaClient=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5104
      - ProblemasApiClient=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5109
      - WorkflowApiClient=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5110
      - UseCustomizationData=True  
      - ApplicationInsights__InstrumentationKey=${INSTRUMENTATION_KEY}
      - OrchestratorType=${ORCHESTRATOR_TYPE}
    ports:
      - "5105:80"   

  problemas.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:80
      - ConnectionString=${SGQ_AZURE_COSMOSDB:-mongodb://nosql.data}
      - Database=ProblemasDb
      - identityUrl=http://identity.api 
      - IdentityUrlExternal=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5105
      - EventBusConnection=${ESHOP_AZURE_SERVICE_BUS:-rabbitmq}
      - EventBusUserName=${ESHOP_SERVICE_BUS_USERNAME}
      - EventBusPassword=${ESHOP_SERVICE_BUS_PASSWORD}       
      - AzureServiceBusEnabled=False
      - ApplicationInsights__InstrumentationKey=${INSTRUMENTATION_KEY}
      - OrchestratorType=${ORCHESTRATOR_TYPE}
      - UseLoadTest=${USE_LOADTEST:-False}
    ports:
      - "5109:80"   # The API Gateway redirects and access through the internal port (80).

  webapigw:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - IdentityUrl=http://identity.api
      - ProblemasUrlHC=http://problemas.api/hc
      - WorkflowUrlHC=http://workflow.api/hc
      - IdentityUrlHC=http://identity.api/hc             
    ports:
      - "5202:80"   
    volumes:
      - ./src/ApiGateways/APIGw-Base:${ESHOP_OCELOT_VOLUME_SPEC:-/app/configuration}
   
  webspa:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:80
      - IdentityUrl=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5105
      - ProblemasUrl=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5109
      - WorkflowUrl=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5110
      - ProblemasUrlHC=http://webapigw/hc
      - IdentityUrlHC=http://identity.api/hc
      - UseCustomizationData=True
      - ApplicationInsights__InstrumentationKey=${INSTRUMENTATION_KEY}
      - OrchestratorType=${ORCHESTRATOR_TYPE}
    ports:
      - "2828:2828"

#  workflow.api:
#    environment:
#      - ASPNETCORE_ENVIRONMENT=Development
#      - ASPNETCORE_URLS=http://0.0.0.0:80
#      - ConnectionString=${SGQ_AZURE_COSMOSDB:-mongodb://nosql.data}
#      - Database=ProblemasDb
#      - identityUrl=http://identity.api
#      - IdentityUrlExternal=http://${ESHOP_EXTERNAL_DNS_NAME_OR_IP}:5201
#      - EventBusConnection=${ESHOP_AZURE_SERVICE_BUS:-rabbitmq}
#      - EventBusUserName=${ESHOP_SERVICE_BUS_USERNAME}
#      - EventBusPassword=${ESHOP_SERVICE_BUS_PASSWORD}       
#      - AzureServiceBusEnabled=False
#      - ApplicationInsights__InstrumentationKey=${INSTRUMENTATION_KEY}
#      - OrchestratorType=${ORCHESTRATOR_TYPE}
#      - UseLoadTest=${USE_LOADTEST:-False}
#    ports:
#      - "5110:80"   # The API Gateway redirects and access through the internal port (80).
  